# Copy to .env — the scanner picks it up from the working directory.
# A GitHub token raises the API rate limit from 60 to 5000 requests/hour.
# Fine-grained token with public-repo read access is enough.
# GH_TOKEN is also accepted; GITHUB_TOKEN wins if both are set.
GITHUB_TOKEN=
