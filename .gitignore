.env
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/
.coverage
coverage.txt
.ruff_cache/
uv.lock
.idea/
.vscode/
# Generated scan outputs
*.json
# Shipped data files, not scan output. Each needs its own exception: the rule
# above is a blanket, and a data file that silently fails to ship degrades
# quietly — an absent platform_icon_hashes.json does not error, it just starts
# accepting Read the Docs' stock favicon as projects' own.
!src/scanner/data/jurisdiction_places.json
!src/scanner/data/platform_icon_hashes.json
*.html
storage/
# Downloaded GeoNames dumps for scripts/build_jurisdiction_gazetteer.py
.gazetteer-cache/
